
fn main() {
    use text_io::scan;
    let board = [[false, false, false]
            ,[false,false,false]
            ,[false,false,false]];
    for i in 0..board.len() {
        println!("Array {:?}", board[i]);
    }
}
